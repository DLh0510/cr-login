name: Build Windows Installer

on:
  push:
    branches: [main, AUTO14199, aitscgc]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Python dependencies
        run: |
          pip install pyinstaller
          pip install -r requirements.txt
      
      - name: Build EXE
        run: pyinstaller --onefile --windowed --name "PromptEngineering-Login" --icon "icon.ico" --add-data "yjgj_foot_logo.png;." main.py

      - name: Create Installer with Inno Setup
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.4
        with:
          path: installer.iss

      - name: Upload Installer
        uses: actions/upload-artifact@v4
        with:
          name: PromptEngineering-Platform-Windows-Installer
          path: Output/*.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller
          pip install -r requirements.txt
      
      - name: Build
        run: pyinstaller --onefile --windowed --name "PromptEngineering-Login" --add-data "yjgj_foot_logo.png:." main.py

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: PromptEngineering-Platform-macOS
          path: dist/PromptEngineering-Login

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller
          pip install -r requirements.txt
      
      - name: Build
        run: pyinstaller --onefile --windowed --name "PromptEngineering-Login" --add-data "yjgj_foot_logo.png:." main.py

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: PromptEngineering-Platform-Linux
          path: dist/PromptEngineering-Login
